# Отчет об ошибках и багах в системе оплаты

## Критические ошибки

### 1. ❌ Отсутствие обработки исключений в `charge_saved_method` (youmoney.py:116)
**Проблема:** Если `Payment.create()` выбросит исключение, попытка списания не будет помечена как failed, и подписка может застрять в состоянии ожидания.

**Место:** `bot/utils/youmoney.py:116`
```python
payment = Payment.create(body, uuid.uuid4())
# Нет try-except блока!
```

**Последствия:**
- Исключение пробрасывается наверх, но попытка в БД остается со статусом "created"
- Подписка может не обновляться, и следующее списание не произойдет
- Нет логирования ошибки для отладки

**Решение:** Добавить обработку исключений и пометить попытку как failed.

---

### 2. ❌ Проблема с обработкой ошибок в `billing_loop` (run.py:275-306)
**Проблема:** Если `charge_saved_method` упадет ДО создания `attempt_id`, попытка не будет создана, но подписка может остаться в неконсистентном состоянии.

**Место:** `bot/run.py:275-306`
```python
attempt_id = billing_db.precharge_guard_and_attempt(...)
if not attempt_id:
    continue
pay_id = youmoney.charge_saved_method(...)  # Может упасть здесь
```

**Последствия:**
- Если исключение произошло после создания `attempt_id`, попытка помечается как failed
- Но если исключение произошло до создания `attempt_id`, попытка не создается, но подписка уже может быть изменена
- Нет гарантии консистентности состояния

**Решение:** Улучшить обработку ошибок и гарантировать атомарность операций.

---

### 3. ❌ Отсутствие валидации `payment_method_id` в `charge_saved_method`
**Проблема:** Функция не проверяет, существует ли и валиден ли `payment_method_id` перед созданием платежа.

**Место:** `bot/utils/youmoney.py:73-116`
```python
def charge_saved_method(..., payment_method_id: str, ...):
    # Нет проверки на пустую строку или None
    body = {
        "payment_method_id": payment_method_id,  # Может быть невалидным
        ...
    }
```

**Последствия:**
- Если токен был удален или невалиден, платеж упадет с ошибкой
- Нет раннего обнаружения проблемы
- Пользователь не получит уведомление о проблеме

**Решение:** Добавить валидацию `payment_method_id` перед созданием платежа.

---

### 4. ❌ Проблема с обработкой `pm_token=None` в webhook для trial платежей
**Проблема:** В сценарии `is_recurring and phase == "trial"` подписка создается с `payment_method_id=pm_token`, но если `pm_token` равен `None`, подписка создается без токена, что делает невозможным автопродление.

**Место:** `bot/handlers/payment_handler.py:1056`
```python
billing_db.subscription_upsert(
    ...
    payment_method_id=pm_token,  # Может быть None
    ...
)
```

**Последствия:**
- Подписка создается без `payment_method_id`
- Автопродление не будет работать
- Пользователь не получит уведомление о проблеме

**Решение:** Проверять `pm_token` перед созданием подписки и логировать предупреждение.

---

### 5. ❌ Проблема с созданием подписки при renewal без `pm_token`
**Проблема:** В сценарии renewal, если `pm_token` отсутствует, но подписка не найдена, создается новая подписка с `payment_method_id=None`, что делает автопродление невозможным.

**Место:** `bot/handlers/payment_handler.py:1128-1134`
```python
billing_db.subscription_upsert(
    ...
    payment_method_id=pm_token,  # Может быть None
    ...
)
```

**Последствия:**
- Создается подписка без возможности автопродления
- Пользователь не получает уведомление
- Проблема обнаруживается только при следующей попытке списания

**Решение:** Не создавать подписку без `pm_token` или логировать критическую ошибку.

---

## Важные баги

### 6. ⚠️ Проблема с обработкой canceled подписок при renewal
**Проблема:** При renewal платеже для canceled подписки система не создает новую подписку, но платеж уже прошел. Пользователь заплатил, но подписка не активирована.

**Место:** `bot/handlers/payment_handler.py:1119-1125`
```python
if canceled_sub:
    logger.warning(...)
    # Не создаём новую подписку, просто логируем
```

**Последствия:**
- Пользователь заплатил, но подписка не активирована
- Деньги списаны, но доступ не предоставлен
- Требуется ручное вмешательство

**Решение:** Создавать новую подписку или активировать существующую canceled подписку.

---

### 7. ⚠️ Отсутствие проверки на дубликаты платежей в webhook
**Проблема:** Хотя есть проверка в Redis, нет дополнительной проверки в БД для защиты от race conditions при одновременной обработке нескольких webhook'ов.

**Место:** `bot/handlers/payment_handler.py:850-872`
```python
# Проверка только в Redis
ok = await yookassa_dedup.should_process(payment_id, status_lc)
if not ok:
    return 200, f"duplicate/no-op status={status_lc}"
# Нет проверки в БД payment_log
```

**Последствия:**
- При сбое Redis возможна двойная обработка
- Возможны race conditions при одновременной обработке
- Могут быть созданы дубликаты подписок или пробных периодов

**Решение:** Добавить проверку в БД `payment_log` перед обработкой.

---

### 8. ⚠️ Проблема с обработкой ошибок при создании ссылок оплаты
**Проблема:** В `_create_links_for_selection` для карты ловится общий `Exception`, а для СБП - только `ForbiddenError` и общий `Exception`. Это может скрыть важные ошибки.

**Место:** `bot/handlers/payment_handler.py:702-730`
```python
try:
    pay_url_card = youmoney.create_pay_ex(...)
except Exception as e:  # Слишком общий catch
    logger.error("Card recurring not allowed — declining tokenless trial: %s", e)
    pay_url_card = None
```

**Последствия:**
- Важные ошибки (например, проблемы с сетью) обрабатываются как "recurring not allowed"
- Пользователь не получает правильное сообщение об ошибке
- Сложно отлаживать проблемы

**Решение:** Различать типы ошибок и обрабатывать их по-разному.

---

### 9. ⚠️ Проблема с обработкой нерекуррентных платежей
**Проблема:** В блоке `else` (не рекуррентный кейс) создается пробный период, но не создается подписка. Это может привести к тому, что пользователь заплатил, но подписка не создана.

**Место:** `bot/handlers/payment_handler.py:1153-1162`
```python
else:
    # Не рекуррентный кейс (включая trial_tokenless): только пробный период.
    trial_hours = int(str(metadata.get("trial_hours") or "72"))
    trial_until = app_db.set_trial(user_id, hours=trial_hours)
    # Нет создания подписки!
```

**Последствия:**
- Пользователь заплатил, но подписка не создана
- После окончания пробного периода доступ прекратится
- Нет механизма автопродления

**Решение:** Создавать подписку даже для нерекуррентных платежей или логировать предупреждение.

---

### 10. ⚠️ Проблема с валидацией `amount_rub` в `create_pay_ex`
**Проблема:** Функция не проверяет формат и валидность суммы перед созданием платежа.

**Место:** `bot/utils/youmoney.py:16-69`
```python
def create_pay_ex(..., amount_rub: str, ...):
    # Нет проверки формата суммы
    body = {
        "amount": {"value": amount_rub, "currency": "RUB"},
        ...
    }
```

**Последствия:**
- Невалидные суммы (отрицательные, слишком большие, неправильный формат) могут быть отправлены в YooKassa
- YooKassa может отклонить платеж, но ошибка не будет обработана правильно
- Пользователь получит неясное сообщение об ошибке

**Решение:** Добавить валидацию суммы перед созданием платежа.

---

## Потенциальные проблемы

### 11. 💡 Проблема с обработкой статуса "waiting_for_capture"
**Проблема:** Статус "waiting_for_capture" обрабатывается, но не создается запись в `payment_log`, что может привести к проблемам при отладке.

**Место:** `bot/handlers/payment_handler.py:864-867`
```python
if status_lc == "waiting_for_capture":
    await yookassa_dedup.should_process(payment_id, status_lc)
    return 200, "ack waiting_for_capture"
# Нет записи в payment_log
```

**Последствия:**
- Нет истории платежей в БД для этого статуса
- Сложно отследить, какие платежи находятся в ожидании
- Нет возможности восстановить состояние после сбоя

**Решение:** Создавать запись в `payment_log` для всех статусов.

---

### 12. 💡 Проблема с обработкой ошибок при сохранении карты
**Проблема:** Если `card_upsert_from_provider` упадет, платеж все равно будет обработан, но карта не будет сохранена.

**Место:** `bot/handlers/payment_handler.py:1019-1024`
```python
if pm_token:
    billing_db.card_upsert_from_provider(...)  # Может упасть
# Продолжаем обработку даже если сохранение карты не удалось
```

**Последствия:**
- Платеж обработан, но карта не сохранена
- Автопродление не будет работать
- Пользователь не получит уведомление

**Решение:** Обрабатывать ошибки сохранения карты и логировать их.

---

### 13. 💡 Проблема с обработкой ошибок при обновлении подписки
**Проблема:** Если `subscription_mark_charged_for_user` вернет `None`, но подписка существует, платеж обработан, но подписка не обновлена.

**Место:** `bot/handlers/payment_handler.py:1099-1104`
```python
updated_sub_id = billing_db.subscription_mark_charged_for_user(...)
if not updated_sub_id:
    # Проверяем canceled подписку, но не проверяем другие случаи
```

**Последствия:**
- Платеж обработан, но `next_charge_at` не обновлен
- Следующее списание может произойти слишком рано или слишком поздно
- Нет уведомления пользователя о проблеме

**Решение:** Улучшить обработку случая, когда подписка не найдена.

---

### 14. 💡 Проблема с обработкой ошибок при отправке уведомлений
**Проблема:** Если `_notify_after_payment` упадет, платеж все равно будет обработан, но пользователь не получит уведомление.

**Место:** `bot/handlers/payment_handler.py:1061, 1145, 1157`
```python
await _notify_after_payment(bot, user_id, code, ...)  # Может упасть
# Продолжаем обработку даже если уведомление не отправлено
```

**Последствия:**
- Платеж обработан, но пользователь не знает об этом
- Пользователь может подумать, что платеж не прошел
- Может потребоваться ручное уведомление

**Решение:** Обрабатывать ошибки отправки уведомлений и логировать их.

---

### 15. 💡 Проблема с обработкой ошибок при добавлении в чат
**Проблема:** Если `membership_invite` упадет, платеж обработан, но пользователь не добавлен в чат.

**Место:** `bot/handlers/payment_handler.py:1063-1066, 1147-1150, 1159-1162`
```python
try:
    asyncio.create_task(membership_invite(user_id))
except Exception:
    pass  # Тихая ошибка
```

**Последствия:**
- Платеж обработан, но пользователь не получил доступ к контенту
- Пользователь может подумать, что сервис не работает
- Требуется ручное добавление в чат

**Решение:** Улучшить обработку ошибок и логировать их.

---

### 16. 💡 Проблема с обработкой BadRequestError в create_pay_ex
**Проблема:** Импортируется `BadRequestError`, но не используется в обработке исключений. Это может привести к тому, что ошибки валидации от YooKassa не будут правильно обработаны.

**Место:** `bot/utils/youmoney.py:5, 61-68`
```python
from yookassa.domain.exceptions.bad_request_error import BadRequestError
...
try:
    payment = Payment.create(body, uuid.uuid4())
except ForbiddenError as e:
    raise
except Exception:  # BadRequestError попадет сюда
    raise
```

**Последствия:**
- Ошибки валидации от YooKassa обрабатываются как общие исключения
- Нет возможности различать типы ошибок
- Сложно отлаживать проблемы с валидацией

**Решение:** Добавить отдельную обработку `BadRequestError`.

---

### 17. 💡 Проблема с отсутствием валидации confirmation_url
**Проблема:** Функция `create_pay_ex` возвращает `payment.confirmation.confirmation_url` без проверки на `None` или пустую строку.

**Место:** `bot/utils/youmoney.py:69`
```python
return payment.confirmation.confirmation_url  # Может быть None
```

**Последствия:**
- Если YooKassa вернет платеж без `confirmation_url`, функция вернет `None`
- Пользователь получит невалидную ссылку
- Нет проверки перед использованием ссылки

**Решение:** Добавить проверку на `None` и валидность URL.

---

### 18. 💡 Проблема с обработкой ошибок при парсинге created_at
**Проблема:** Если парсинг `created_at` упадет, используется текущее время, но это может привести к неправильному расчету `next_charge_at` при задержке webhook'а.

**Место:** `bot/handlers/payment_handler.py:1044-1046`
```python
except Exception as e:
    logger.warning("Failed to parse payment created_at %s: %s", payment_created_at, e)
    payment_created_dt = now_msk()  # Используем текущее время
```

**Последствия:**
- При задержке webhook'а `next_charge_at` может быть рассчитан неправильно
- Пользователь может получить неправильную дату окончания подписки
- Может привести к преждевременному или запоздалому списанию

**Решение:** Улучшить обработку ошибок парсинга и логирование.

---

### 19. 💡 Проблема с использованием hardcoded email в receipt
**Проблема:** В `create_pay_ex` используется hardcoded email "admin@google.com" для всех платежей.

**Место:** `bot/utils/youmoney.py:42`
```python
"receipt": {
    "customer": {"email": "admin@google.com"},  # Hardcoded email
    ...
}
```

**Последствия:**
- Все чеки отправляются на один email
- Нет возможности отправлять чеки пользователям
- Может быть проблемой с налоговым законодательством

**Решение:** Использовать email пользователя или убрать receipt, если email не требуется.

---

### 20. 💡 Проблема с отсутствием проверки на дубликаты подписок
**Проблема:** В `subscription_upsert` есть проверка на активные подписки, но нет проверки на дубликаты при создании новой подписки для того же пользователя и тарифа.

**Место:** `bot/utils/billing_db.py:589-599`
```python
rec = (
    s.query(Subscription)
    .with_for_update()
    .filter(
        Subscription.user_id == user_id, 
        Subscription.plan_code == plan_code,
        Subscription.status == "active"
    )
    .first()
)
# Если нет активной, проверяем любую (для обновления canceled)
```

**Последствия:**
- Возможны дубликаты подписок для одного пользователя
- Может привести к множественным списаниям
- Сложно отследить, какая подписка активна

**Решение:** Улучшить логику проверки существующих подписок.

---

## Рекомендации по исправлению

1. **Добавить валидацию входных данных** во всех функциях создания платежей
2. **Улучшить обработку ошибок** во всех критических местах
3. **Добавить логирование** для всех ошибок и предупреждений
4. **Добавить проверки консистентности** состояния после операций
5. **Улучшить обработку edge cases** (canceled подписки, отсутствие токенов и т.д.)
6. **Добавить мониторинг** для отслеживания проблем в продакшене
7. **Добавить unit-тесты** для всех критических сценариев
8. **Добавить интеграционные тесты** для проверки полного цикла оплаты
9. **Добавить валидацию всех возвращаемых значений** от YooKassa API
10. **Улучшить обработку различных типов исключений** от YooKassa

